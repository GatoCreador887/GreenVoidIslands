package gatocreador887.greenvoidislands.client;

public class ClientEventHandler {
	
	/*
	 * @SubscribeEvent public static void onPlaySoundEvent(PlaySoundEvent event) {
	 * Minecraft minecraft = Minecraft.getMinecraft(); EntityPlayerSP player =
	 * minecraft.player; if (player != null && player.world.provider instanceof
	 * GVIWorldProvider && (event.getSound().getSoundLocation().equals(new
	 * ResourceLocation("minecraft:music.creative")) ||
	 * event.getSound().getSoundLocation().equals(new
	 * ResourceLocation("minecraft:music.game"))) &&
	 * !minecraft.getSoundHandler().isSoundPlaying(PositionedSoundRecord.
	 * getMusicRecord(GVISoundManager.gvi_music))) { event.setResultSound(null);
	 * player.world.playSound(player.posX, player.posY, player.posZ,
	 * GVISoundManager.gvi_music, SoundCategory.MUSIC, 100000.0F, 1.0F, false); } }
	 */
	
}
